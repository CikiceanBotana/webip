# Reproducibility: every dependency is pinned to an exact version.
# This makes `npm install <pkg>` behave like `npm install --save-exact <pkg>`
# so no caret range can ever leak into package.json.
save-exact=true
